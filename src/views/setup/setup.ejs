<%
  const content = include('./setup-content', { error, usernameValue, minPasswordLength, minUsernameLength });
%>
<%- include('../partials/layout', { ...locals, body: content, pageTitle: 'Set Up Administrator' }) %>
