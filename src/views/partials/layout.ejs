<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>PPCollection</title>
  <link rel="stylesheet" href="/static/css/styles.css">
</head>
<body>
  <header class="topbar">
    <div class="container">
      <h1><a href="/">PPCollection</a></h1>
      <% if (user) { %>
      <nav class="navbar">
        <a href="/firearms" class="nav-link">Library</a>
        <a href="/firearms/export" class="nav-link" download>Export</a>
        <a href="/profile" class="nav-link">Profile</a>
        <form method="post" action="/logout" class="nav-logout">
          <button class="btn btn-secondary" type="submit">Logout</button>
        </form>
      </nav>
      <% } %>
    </div>
  </header>
  <main class="container">
    <%- body %>
  </main>
  <footer class="footer container">
    <small>Local-only collection. Data stored in SQLite.</small>
  </footer>
</body>
</html>

